package uk.wycor.starlines.domain.geometry;

class SzudzikTest {
/*

    public static void main(String[] args) {
        int i = 67108864;
        long pairedNumber = pair(i, i);
        Pair unpair = unpair(pairedNumber);
        System.out.println("Paired Number is    " + pairedNumber);
        System.out.println("First unpaired Number is    " + unpair.a + "    Second unpaired Number is    " + unpair.b);
    }
 */

    // private static Stream<Arguments>
}